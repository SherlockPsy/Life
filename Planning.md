The Database Schema (SQL): I can generate the raw PostgreSQL schema to create the Tables for S1 through S6, including the specific constraints for the "Time/Distance" physics.

The Drift Engine Logic (Pseudo-Code): We can define the precise algorithm for the "Catch-Up Loop" that runs when you return after 10 days of silence.

The Prompt Engineering Strategy: We can design the actual System Prompts for the LLM that enforce the "Pulse" and "Salience" rules.

Copilot Instructions

